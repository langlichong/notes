- TLV

  > **Type (T)**: a byte or a sequence of bytes that identifies the type of the element.
  >
  > **Length (L)**: a byte or a sequence of bytes that specifies the length of the value.
  >
  > **Value (V)**: a sequence of bytes that contains the actual value of the element.

- JSON
- NDJSON
- **BSON** 
- **ASN.1 (Abstract Syntax Notation One)**
- **BER (Basic Encoding Rules)**
- **MessagePack**
-  **Protocol Buffers**
- **Avro**
- Parquet